// "Stories That Inspire" (Voices of EIT) section. Quotes live in data/students/testimonials.ts.

export const storiesSection = {
  eyebrow: "Voices of EIT",
  title: ["Stories", "That Inspire"],
  /** Handwritten phrase on the right. */
  script: ["Real", "People", "Real Impact"],
  /** Time each story stays on screen, in milliseconds. */
  intervalMs: 7000,
};

/** Ids from data/students/testimonials.ts, in display order. Empty = show all. */
export const featuredStoryIds: string[] = [];
