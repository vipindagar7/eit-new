// Content for this area lives here — never inside components.
// Intentionally empty until real content is supplied. Do not add placeholder statistics.

import type { LeadershipMessage, Testimonial } from "@/types";

/** "Students Speak" */
export const studentsSpeak: Testimonial[] = [];

/** "Recruiters Speak" */
export const recruitersSpeak: Testimonial[] = [];

/** Message from HoD and Training & Placement. */
export const placementMessages: LeadershipMessage[] = [];
