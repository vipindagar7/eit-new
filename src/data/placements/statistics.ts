// Content for this area lives here — never inside components.
// Intentionally empty until real content is supplied. Do not add placeholder statistics.

export interface PlacementStatistic {
  id: string;
  label: string;
  value: string;
  academicYear?: string;
  note?: string;
}

export const placementStatistics: PlacementStatistic[] = [];
