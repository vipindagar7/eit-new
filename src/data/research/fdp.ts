// Content for this area lives here — never inside components.
// Intentionally empty until real content is supplied. Do not add placeholder statistics.

export interface FacultyDevelopmentProgram {
  id: string;
  title: string;
  date?: string;
  organiser?: string;
  description?: string;
}

export const facultyDevelopmentPrograms: FacultyDevelopmentProgram[] = [];
