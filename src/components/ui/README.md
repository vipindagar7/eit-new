# components/ui

shadcn/ui primitives. Add them one at a time, only when a section needs them:

    npx shadcn@latest add button

Magic UI components are added the same way through the registry configured in `components.json`:

    npx shadcn@latest add @magicui/marquee
